# Loops

user = int(input('Enter a number: '))
user = int(input('Enter a second number: '))

while user + user:
    user = int(input('Enter a number: '))
    user = int(input('Enter a second number: '))
    print ('This is the added amount', user)
    
print ('Goodbye!')






